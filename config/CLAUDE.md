# config

Top-level configuration surfaces for `qualitative_coding`.

Use this subtree for project-level config and templates that shape pipeline
behavior. Keep operational defaults here rather than hardcoding them in the
pipeline engine.

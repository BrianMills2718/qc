# Evaluation Harness — Design

**The keystone for a public, state-of-the-art qualitative coding system.**

*Version 0.1 (design) — 2026-06-19*

> **Why this exists.** Per `PROJECT_THEORY_AND_GOALS.md` (Ambition), the product
> goal is to be provably SOTA on the measurable rigor dimensions and at
> expert-parity on interpretation. "Provably" is the operative word: **you cannot
> beat a state of the art you cannot measure.** This harness is simultaneously
> (a) the *evidence* for every SOTA claim and (b) the *feedback loop* that makes
> the system better. It closes **INV-3** (validity adjudication separate from
> consistency) and operationalizes **INV-5** (bias). It is roadmap item #1.
>
> **Build on, don't hand-roll.** This harness will be built on `prompt_eval`
> (`~/projects/prompt_eval`) — frozen case sets, evaluators (`llm_judge`,
> `kappa`, `exact_match`, `contains`), bootstrap CIs, Welch's test, instruction
> search — and `llm_client` observability. *Current integration is minimal:* one
> optimization script imports `prompt_eval` (`scripts/optimize_thematic_prompt.py`)
> and `PipelineContext.prompt_overrides` exists, with rendered custom overrides
> now bookended by a repo-owned instruction/data-separation wrapper. **Built so far (Phase 0):**
> `make bench ID=<project_id>` / `qc_cli.py bench <project_id>` →
> `qc_clean/core/bench.phase0_scorecard` (D1
> grounding + D2 coverage/coded-segment-rate accounting, externally supplied D4/D6/D8/D9 with D4/D6/D8/D9 score-time protocol guards when protocol files are supplied, confidence-calibration protocol validation/result preflight/score-time guard and scorecard accounting, and D7/INV-7 fixture scores,
> D3/D7 human-ceiling exact-metric comparison when versioned gold packages supply human-human recall/precision/F1, D10 cost/latency from observability rows plus last-run wall-clock metadata, `make validate-adjudication-protocol` for pre-label protocol metadata validation, `make adjudication-protocol-preflight` for protocol/sample package matching before labeling, `make adjudication-sample` for unlabeled human/expert review packets, `make validate-adjudication-responses` for completed-response shape/completeness validation, `make adjudication-response-preflight` for completed response protocol/sample provenance checks before import, `make import-adjudication-responses` for explicit D3/D7 package import from valid completed responses with optional import-time preflight guard inputs, `make write-phase0-adjudication-package` for strict manifest assembly from those versioned package inputs, and INV-4 theoretical-sampling candidate/result export plus protocol validation/preflight surfaces for concrete candidate/result packages, with scorecard input/config hashes and optional versioned `benchmark_results/` artifact packaging including a D10 timing file; deterministic, LLM-free except optional live fixture/retrieval exports). **Not yet built:** the full `prompt_eval`-backed
> suite — frozen case sets, the QC-specific evaluators below, gold-vs-agreement
> (D3), held-out D7 benchmark runs, populated D6/D8/D9 protocols, populated calibration benchmark, broader held-out live prompt-injection runs, baselines — remains a design target. Do not
> reimplement scoring, statistics, or experiment tracking — extend `prompt_eval`.

---

## 1. What it must measure

Each dimension maps to a SOTA claim and (where relevant) an invariant. The bar is **"≥ the relevant baseline / human ceiling, with a confidence interval that excludes the baseline."** A claim is licensed only for dimensions that pass that bar.

| # | Dimension | What it answers | Invariant | SOTA bar (target) |
|---|-----------|-----------------|-----------|-------------------|
| D1 | **Evidentiary grounding** | Do quotes resolve to the exact source span? | INV-1 | ≥0.95 exact-span resolution; ~0 unresolvable. (Machine advantage via hard enforcement.) |
| D2 | **Coverage** | Did every textual unit get a coding decision (incl. null)? | INV-8 | 1.0 over the segment universe. Edge vs *humans* (who rarely code exhaustively); **not** vs all tools — MAXQDA 26.2 (Mar 2026) ships segment-level AI coding. So the defensible edge is exhaustive coverage *with a scored, span-anchored denominator* (`examined_rate` + `make bench`), not segment-level coding per se. |
| D3 | **Application validity vs gold** | Do code→segment assignments match an adjudicated gold? | INV-3 | Agreement with gold ≥ human–human agreement on the same task (κ/α in the human band, see §3). |
| D4 | **Codebook quality** | Are the codes clear, specific, useful, grounded? | — | Blind expert ratings ≥ human-generated codebook. |
| D5 | **Reliability (consistency, not validity)** | Is the system self-consistent across passes? | — | Report LLM-pass agreement + stability with bootstrap CIs. Necessary, not sufficient. |
| D6 | **Bias** | Does coding track identity cues rather than meaning? | INV-5 | Counterfactual identity-swap code-change rate ≈ 0; stratified error parity. ≤ human bias where measurable. |
| D7 | **Disconfirmation** | Does it find the contrary evidence humans find? | INV-2/6 | Negative-case recall/precision vs human-identified disconfirming evidence ≥ human. |
| D8 | **GT fidelity** | Constant comparison / category development / saturation, if claimed | INV-4 | Expert-rubric acceptance (only if the GT path claims theory generation). |
| D9 | **Interpretive depth (the hard wall)** | Latent meaning, tone, conversational context | — | Blind expert *preference* parity (≈50/50 system vs human), augmented by human-in-the-loop. **Target parity, not dominance.** |
| D10 | **Cost / latency** | $ and wall-clock per transcript | — | Report; machine advantage is expected, not the headline. |

**Honest sequencing of the claim:** SOTA is *earned per dimension*. We expect to clear it decisively on D1–D2, D5, D7, D10 and credibly on D3–D4; D9 is parity-at-best near-term; D6/D8 are frontiers we measure before claiming.

## 2. Metrics (concrete and computable)

- **D1 grounding:** `% quotes whose normalized text resolves to a unique anchored span (doc_id + char offsets + hash)`; count of unresolvable / multiply-resolvable / hallucinated quotes. **Implemented** — `qc_clean/core/grounding.verify_grounding` (INV-1 landed); surfaced via `make bench` with a local Wilson interval and MCP `qc_grounding_report`.
- **D2 coverage:** **implemented** — `qc_clean/core/segmentation.compute_coverage` reports both *traversal* coverage (`covered/total`) and, under **exhaustive coding** (`--exhaustive`), *examined-and-judged* coverage (`examined_rate`, `coded_segments`) over the char-anchored segment universe (INV-8). `make bench` also reports `coded_segment_rate` as `coded_segments / examined_segments`, with a local Wilson interval; when no decisions exist, the denominator is undefined rather than treated as zero. The defensible denominator the harness needs.
- **D3 application validity:** against adjudicated gold on the **same units** — Cohen's κ (2 raters) / **Krippendorff's α** (handles multi-label, missing, boundary disagreement), precision/recall/F1 on code labels, and **span alignment via IoU + Modified Hausdorff Distance** (the LLMCode metrics). **Scoring substrate implemented:** `phase0_scorecard` / `make bench D3_GOLD=d3_gold.json` reports exact code/source-anchor TP/FP/FN, precision, recall, F1, recall/precision Wilson intervals, a deterministic exact-key F1 bootstrap interval, local same-code/same-document char-span IoU plus discrete Modified Hausdorff diagnostics, exact-key binary `system_gold_agreement` metadata with percent agreement, Cohen's κ, Gwet's AC1, exact-key Krippendorff's α, and prevalence, and a `human_ceiling_comparison` section when a versioned D3 package supplies human-human recall/precision/F1 metrics or chance-corrected human agreement metadata (κ/α/AC1). Gold files may be raw anchor lists, objects with `application_gold` / `code_applications`, or versioned schema_version=1 D3 gold-set packages validated by `make validate-d3-gold GOLD=gold_set.json`; when a versioned package is used, the scorecard includes compact `gold_provenance` with dataset/split/hash/freeze/adjudication metadata. This exact/span-distance scorer is not full D3: full semantic/multi-label Krippendorff's α, semantic near-match agreement, populated human-ceiling agreement, and expert parity remain future work. The unit-of-analysis fix the IRR caveat (theory doc §11) demands. **Report a prevalence-robust coefficient (Gwet's AC1) alongside κ:** qualitative codes are often rare, and κ collapses under skewed prevalence even at high raw agreement (the 2026 PLOS study reported κ≈0.34 despite high agreement, and reports AC1 for exactly this reason). Make prevalence attenuation a first-class reported quantity, not a caveat.
- **D3 baseline comparison substrate:** when D3 gold is available, `phase0_scorecard` / `make bench D3_BASELINES=d3_baselines.json` / `scripts/bench_phase0.py --d3-baselines-file` can score externally supplied `application_baselines` records against the same exact code/source-anchor gold as the system, reporting baseline TP/FP/FN, recall, precision, F1, recall/precision Wilson intervals, F1 bootstrap intervals, system-minus-baseline deltas, and local paired exact-key bootstrap intervals. D3 baseline files may be legacy raw lists, legacy objects with `application_baselines`, or versioned schema_version=1 D3 baseline packages validated by `make validate-d3-baseline-package PACKAGE=...`; recognized versioned packages validate before scoring. External baseline files are applied in memory and do not mutate saved project state. This is still exact-key local accounting; no live baseline run or held-out D3 superiority/non-inferiority result exists.
- **D4 codebook quality:** `llm_judge` rubric (clarity / specificity / usefulness / grounding, 0–1 each) **plus** a blind human expert panel; report both and their agreement. **Protocol/scoring substrate implemented:** `make validate-d4-codebook-quality-protocol PROTOCOL=protocol.json` validates pre-evaluation D4 protocol metadata for codebook artifact hashes, blinding, evaluator plan, rubric metrics, held-out gates, and success criteria. `make d4-codebook-quality-preflight PROTOCOL=protocol.json QUALITY=quality.json` checks concrete D4 result files against that protocol, including optional result-file hash locks, evaluator type/count, target scopes, and individual-code `code_id` coverage. `phase0_scorecard` / `make bench D4_PROTOCOL=protocol.json CODEBOOK_QUALITY=quality.json` can run the same preflight at score time before output/artifact writes and score externally supplied rubric outcomes, reporting overall and by-evaluator-type summaries for clarity, specificity, usefulness, grounding, overall mean, and deterministic local bootstrap intervals for rubric means. This is local protocol/accounting only: no LLM-judge generation, blind expert-panel collection, or expert agreement evidence exists until the full benchmark protocol supplies those rubric outcomes.
- **D5 reliability:** existing `project irr` / `project stability`, labeled *codebook-discovery* vs *application-level* agreement. **Scoring substrate implemented:** `project irr` now computes Gwet's AC1 alongside κ/Fleiss for codebook-discovery, positive segment x code application cells, and segment-decision rows; `make bench` surfaces those values plus rating prevalence tables and deterministic local row-bootstrap intervals for percent agreement and AC1 when `ProjectState.irr_result` is present. Application-level reporting requires INV-8 and includes both positive segment x code application agreement and segment-decision agreement (`coded` / `no_code` / `not_examined`). Remaining D5 work: human IRR comparison and any prompt_eval-backed public benchmark statistics. (GT reliability now measures the actual constant-comparison stage — fixed; see theory doc §11.)
- **D6 bias:** (i) stratified error rate by respondent attribute where ethical; (ii) **counterfactual masking/swap** — hold substantive text constant, vary identity markers, measure code-change rate (target ≈0). **Protocol/scoring substrate implemented:** `make validate-d6-bias-protocol PROTOCOL=protocol.json` validates pre-run schema_version=1 D6 protocol metadata for ethical attribute policy, frozen case-set metadata, configured stratified/counterfactual dimensions, held-out gates, and success criteria. `make d6-bias-preflight PROTOCOL=protocol.json STRATIFIED=bias_stratified.json COUNTERFACTUAL=bias_counterfactual.json` checks concrete result files against that protocol, including required configured files, unexpected files, optional SHA-256 locks, stratified attributes, and stratified surfaces. `phase0_scorecard` / `make bench D6_PROTOCOL=protocol.json BIAS_COUNTERFACTUAL=bias.json BIAS_STRATIFIED=bias_stratified.json` runs the same D6 preflight at score time before output/artifact writes and includes a passing preflight report in `_meta.preflight_reports.d6_bias`; without `D6_PROTOCOL`, the local D6 scorecards remain unguarded accounting surfaces. Counterfactual scoring reports invariant-case code-change rate with a Wilson interval, mean code-set Jaccard distance with a deterministic local bootstrap interval, changed case IDs, and attribute-group summaries with code-change-rate Wilson intervals and mean-Jaccard bootstrap intervals. Stratified scoring reports total/correct/incorrect counts, accuracy/error rates with Wilson intervals, error case IDs, per-attribute/per-group summaries, per-surface summaries, and per-attribute max error-rate gaps. These are local protocol/preflight/accounting substrates only: no populated bias audit, causal proof, or bias-free claim exists until ethical stratification metadata, frozen counterfactual cases, and correctness labels are generated/run and evaluated through the full benchmark protocol.
- **D7 disconfirmation:** recall/precision of system negative cases against a human-built gold set of disconfirming passages. **Scoring substrate implemented:** `phase0_scorecard` / `make bench` computes exact target-claim/source-anchor TP/FP/FN, recall, precision, F1, 95% Wilson intervals for recall/precision, deterministic exact-key F1 bootstrap intervals, exact-key binary `system_gold_agreement` metadata with percent agreement, Cohen's κ, Gwet's AC1, exact-key Krippendorff's α, and prevalence, and a `human_ceiling_comparison` section when a versioned D7 package supplies human-human recall/precision/F1 metrics. Gold scoring runs when `ProjectState.config.extra["disconfirmation_gold"]` contains adjudicated contrary-evidence anchors, or when an external gold JSON file is supplied via `make bench ID=<project> GOLD=gold.json` / `scripts/bench_phase0.py --gold-file`. Gold files may be raw anchor lists, objects with `contrary_evidence`, or versioned schema_version=1 D7 gold-set packages validated by `make validate-d7-gold GOLD=gold_set.json`; when a versioned package is used, the scorecard includes compact `gold_provenance` with dataset/split/hash/freeze/adjudication metadata. It can also score externally supplied baseline predictions from `ProjectState.config.extra["disconfirmation_baselines"]` or `make bench ID=<project> BASELINES=baselines.json` / `scripts/bench_phase0.py --d7-baselines-file`, reporting baseline TP/FP/FN, recall, precision, F1, recall/precision Wilson intervals, F1 bootstrap intervals, system-minus-baseline deltas, and local paired exact-key bootstrap intervals for recall/precision/F1 deltas; raw baseline lists remain supported, and recognized versioned D7 baseline packages validate before scoring. `make run-d7-retrieval ID=<project> OUTPUT=predictions.json [MODE=lexical_bm25|embedding_hybrid]` exports configured retrieval-mode candidates as baseline-compatible predictions for that same `BASELINES=` path with project/corpus/state/trace/budget provenance; `make run-d7-live-baseline ID=<project> OUTPUT=live_baseline.json MODEL=<model>` writes an opt-in live candidate-selection baseline package over bounded retrieval candidates with model/trace/budget/prompt-hash provenance for the same `BASELINES=` path; `make validate-d7-baseline-package PACKAGE=baseline.json` validates retrieval and live-baseline prediction packages without requiring a comparison protocol; `make validate-d7-comparison-protocol PROTOCOL=protocol.json` validates pre-run comparison metadata, `make d7-comparison-preflight PROTOCOL=protocol.json GOLD=gold.json PREDICTIONS="a.json b.json"` checks a versioned D7 gold package and retrieval or live-baseline prediction packages against that protocol, and `make compare-d7-retrieval ID=<project> GOLD=gold.json PREDICTIONS="a.json b.json" [PROTOCOL=protocol.json]` scores multiple exported packages in one exact-span local comparison report while optionally enforcing the same preflight as a score-time guard. External gold and baseline files are applied in memory and do not mutate saved project state. The D7 baseline package validator, protocol/preflight/guard surfaces, and live candidate-selection package generator are orchestration/provenance only. **Not yet done:** no held-out D7 gold set has been populated/run, no committed live baseline result exists, no retrieval/live-baseline comparison has been run on held-out data and prompt_eval-tested, and this score is exact-span only.
- **INV-7 prompt-injection fixture outcomes:** `phase0_scorecard` / `make bench` reports `prompt_injection_inv7` as `not_available` until fixture results are supplied through `ProjectState.config.extra["prompt_injection_evaluations"]`, `make bench ID=<project> PROMPT_INJECTION=inv7.json`, or `scripts/bench_phase0.py --prompt-injection-file`. `make run-inv7-fixtures OUTPUT=inv7.json` writes a deterministic structural-boundary schema_version=1 fixture package for that scorecard path, `make validate-inv7-live-protocol PROTOCOL=protocol.json` validates pre-run live protocol metadata, `make run-inv7-live-fixtures OUTPUT=inv7_live.json MODEL=<model>` writes opt-in live model canary outcomes in the same package-compatible shape with exact prompt hashes, `make inv7-live-preflight PROTOCOL=protocol.json PACKAGE=inv7_live.json` checks a live result package against the registered protocol before scoring, and `make validate-inv7-package PACKAGE=inv7.json` validates package-level result metadata. When supplied, the scorecard reports pass/fail counts, pass rate and attack-success rate with Wilson intervals, failed fixture IDs, and per-surface/per-attack-type summaries with the same interval metadata. `docs/benchmarks/inv7_live_canary_2026_06_22/` contains one committed/scored built-in live canary artifact. This is a measurement/provenance substrate only: the structural runner checks prompt construction, the live protocol/preflight/package validators check metadata shape and package matching, and the live runner checks a small canary set; this is not held-out prompt-injection robustness evidence.
- **INV-7 custom prompt governance:** runtime prompt overrides use declared
  placeholder policies and rendered override prompts are wrapped with a
  repo-owned data-boundary reminder after operator-authored text. This is
  prompt-construction governance only, not a scored robustness result or model-
  obedience proof.
- **D8 GT fidelity:** expert rubric on constant comparison, category property/dimension development, memo quality, saturation justification. **Protocol/scoring substrate implemented:** `make validate-d8-gt-fidelity-protocol PROTOCOL=protocol.json` validates pre-evaluation D8 protocol metadata for corpus/state/GT-artifact hashes, evaluator plan, rubric metrics, held-out gates, target scopes, and success criteria. `make d8-gt-fidelity-preflight PROTOCOL=protocol.json GT_FIDELITY=gt_fidelity.json` checks concrete D8 result files against that protocol, including optional result-file hash locks, evaluator type/count, target scopes, and targeted-scope `artifact_id` coverage. `phase0_scorecard` / `make bench D8_PROTOCOL=protocol.json GT_FIDELITY=gt_fidelity.json` can run the same preflight at score time before output/artifact writes and score externally supplied rubric outcomes, reporting metric summaries for constant comparison, category development, memo quality, saturation justification, overall mean, evaluator-type/scope summaries, and deterministic local bootstrap intervals for rubric means. This is local protocol/accounting only: no rubric generation, expert-panel collection, methodological saturation proof, full-GT evidence, or expert-rubric acceptance exists until the full benchmark protocol supplies those outcomes.
- **D9 interpretive depth:** forced-choice blind preference (expert picks the better of system vs human code/theme for latent-meaning items); report win rate with CI; parity = CI spans 0.5. **Protocol/scoring substrate implemented:** `make validate-d9-interpretive-preference-protocol PROTOCOL=protocol.json` validates pre-evaluation D9 protocol metadata for corpus/state/comparison-artifact hashes, blinding, evaluator plan, target criteria/surfaces, held-out gates, non-inferiority margin, and success criteria. `make d9-interpretive-preference-preflight PROTOCOL=protocol.json PREFERENCE=preference.json` checks concrete result files against that protocol, including optional result-file hash locks, evaluator type/count, target criteria, target surfaces, and planned case count. `phase0_scorecard` / `make bench D9_PROTOCOL=protocol.json PREFERENCE=preference.json` can run the same preflight at score time before output/artifact writes and score externally supplied forced-choice outcomes, reporting system wins, human wins, ties, tie rate with a Wilson interval, system preference rate among non-ties with a Wilson interval, evaluator/criterion summaries with the same interval metadata, a passing `_meta.preflight_reports.d9_interpretive_preference`, protocol/result input hashes, and a `non_inferiority_assessment` using the supplied protocol metadata. The assessment reports system-minus-human preference-rate point/CI and applies the lower-bound-above-`-margin` rule from §7. This is local protocol/accounting only: no preference-case generation, expert-panel collection, or parity evidence exists until the full benchmark protocol supplies populated held-out outcomes.
- **D10 cost/latency:** **scoring substrate implemented** — `make bench` reports `cost_latency_d10` from real `llm_client` `llm_calls` rows, optional matching `tool_calls` rows, and `wall_clock_d10` from the last local `project run` metadata. Default cost matching uses the local project trace prefix (`qualitative_coding/project/<project_id>`), with `TRACE_ID=` / `--trace-id` for exact non-default traces and `OBS_DB=` / `--observability-db` for DB path overrides. Missing DB, matching rows, or run timing reports `not_available`; costs and wall-clock durations are never estimated from `ProjectState`. Existing LLM-only fields remain LLM-only; `tool_calls` and `combined_observed_*` fields add observed tool cost/duration when real tool rows match. `wall_clock_d10.duration_s` is end-to-end elapsed time for the last local run, not a public benchmark runtime. Phase 0 artifact packages include `timing_d10.json` with these local D10 sections, non-sensitive runtime environment metadata, and a manifest hash, but public benchmark timing evidence still requires frozen datasets and baseline/comparator runs under controlled conditions.
- **Confidence calibration:** **protocol/preflight/scoring substrate implemented** — `make validate-confidence-calibration-protocol PROTOCOL=protocol.json` validates pre-evaluation calibration protocol metadata for corpus/state/prediction-artifact hashes, label-source plan, target surfaces, confidence source, held-out gates, outcome metrics, and success criteria. `make confidence-calibration-preflight PROTOCOL=protocol.json CALIBRATION=calibration.json` checks concrete calibration result files against that protocol, including optional result-file hash locks, label-source/evaluator metadata, target surfaces, and planned item count. `phase0_scorecard` / `make bench CONFIDENCE_PROTOCOL=protocol.json CALIBRATION=calibration.json` can enforce that preflight at score time before output/artifact writes and score externally supplied confidence/correctness records, reporting accuracy with Wilson interval, mean confidence, Brier score with local bootstrap interval, fixed 10-bin expected calibration error with local bootstrap interval, bin summaries with bin-level accuracy intervals, and per-surface summaries with the same interval metadata. This is local protocol/accounting only: the system's confidence fields remain ordinal self-reports until populated held-out correctness labels are scored under a frozen benchmark protocol.

## 3. Gold standards and datasets

**The human ceiling (target band).** Published inter-coder agreement: **κ ≈ 0.72–0.82 for inductive coding schemes, 0.58–0.73 for unconstrained schemes.** "Expert parity" (D3) = agreement-with-gold in/above that band; "SOTA" = ≥ the human–human agreement measured on the *same* corpus (compute it; don't assume).

**Reuse released gold (fast, head-to-head, but contamination-flagged):**
- **LLMCode** (github.com/PerttuHamalainen/LLMCode) — human-annotated data + tooling that compares LLM to human codes with IoU / Modified Hausdorff. Closest ready-made evaluator; reuse its metrics and data.
- **HICode** (github.com/mianzg/HICode) — 3 datasets with human-constructed themes; reported **P=0.72 / R=0.74** — a concrete head-to-head target for hierarchical coding.
- **LOGOS** (arXiv:2509.24294) — 5 corpora + a 5-dimensional metric and an expert-developed schema (≈80.4% alignment reported) for GT-style evaluation.
- **Design rule (not just a caveat):** public datasets may be in model training data → **train/test contamination**. Therefore public sets are **regression/comparator suites only** (head-to-head vs HICode/LOGOS/etc.); the **headline SOTA/parity claims must rest on fresh in-house held-out gold** the models have never seen. "Contamination-checked" means: search for verbatim overlap, prefer post-cutoff data, and treat any public-set result as a comparator, never as the headline.

**Build a small in-house adjudicated gold set (for headline claims):** follow the expert-consensus-panel method — ≥2 qualified coders independently code, a third adjudicates disagreements, producing gold codes with labels, definitions, supporting anchored spans, and a recorded human–human agreement (the ceiling). Target the actual product domain (interviews/focus groups). Keep it held-out and prompt-frozen. The repo now has versioned D3 and D7 gold-set package validators (`scripts/validate_d3_gold_set.py` / `make validate-d3-gold`, `scripts/validate_d7_gold_set.py` / `make validate-d7-gold`) for application and contrary-evidence anchors, `make validate-adjudication-protocol PROTOCOL=protocol.json` validates pre-registered protocol metadata, `make adjudication-protocol-preflight PROTOCOL=protocol.json SAMPLE=sample.json` checks protocol/sample matching before labeling, `make adjudication-sample ID=<project_id> OUTPUT=sample.json` can export an unlabeled schema_version=1 review packet over applications, claims, negative cases, and relationships, `make validate-adjudication-responses PACKAGE=sample.json` can validate completed response package shape/completeness, `make adjudication-response-preflight PROTOCOL=protocol.json SAMPLE=sample.json RESPONSES=responses.json` can check completed response provenance against the registered protocol and sample before import, `make import-adjudication-responses ... PREFLIGHT_PROTOCOL=protocol.json PREFLIGHT_SAMPLE=sample.json` can convert valid completed code-application/negative-case responses into D3/D7 package inputs while running that preflight at the import boundary, and `make write-phase0-adjudication-package ...` can assemble a strict Phase 0 manifest from those versioned package files. No human-populated held-out package has been created or scored; these are protocol/provenance artifacts unless populated with real adjudication labels under a documented protocol.

## 4. Baselines to beat

- **Generic single-prompt ChatGPT** (the "just ask an LLM" baseline most practitioners use).
- **This system** (the candidate).
- **Commercial AI coding** — ATLAS.ti Intentional AI Coding / MAXQDA AI Assist, where API access permits.
- **Human-only** — the gold panel's own inter-coder agreement = the ceiling for D3/D9.
- **Task-matched research prototypes** — HICode, LOGOS, TAMA/Auto-TA, re-run on shared data where reproducible.

## 5. Protocol (pre-registered before any run)

- **Datasets & domains:** ≥3 corpora, ≥2 domains, including ≥1 *messy* transcript set (disfluencies, overlap, timestamps).
- **Unit of analysis:** fixed in advance — turn-level for deductive application; researcher-delimited segment for inductive (requires the INV-8 segment universe to be defensible).
- **Blinding:** raters blind to whether codes are human- or machine-generated (D4, D9).
- **Frozen + hashed:** all prompts and model ids frozen and hashed before evaluation; no tuning against the test sets; held-out gold.
- **Pre-registered thresholds:** the §1 bars are written down *before* results, so nothing is graded post hoc.
- **Statistics:** bootstrap CIs and Welch's test (via `prompt_eval`); report prevalence and CIs alongside every κ/α; correct for multiple comparisons across dimensions.
- **Multi-label & near-synonym handling:** a reconciliation rubric for lexically-different-but-semantically-equivalent codes (don't penalize "trust" vs "confidence in system" as disagreement without check).

## 6. Infrastructure

- **`prompt_eval`** owns experiments, frozen case sets, evaluators, optimization (`instruction_search`), persistence, and stats. New QC-specific evaluators (grounding-rate, IoU/Hausdorff span alignment, coverage, counterfactual-swap, disconfirmation recall) are added as `prompt_eval` evaluator functions, not bespoke scripts.
- **`llm_client`** owns the runs and observability (cost/tokens/latency per `task`/`trace_id`).
- **QC surface:** current surfaces are `qc_cli.py bench <project_id>` and `make bench ID=<project_id>` for the deterministic Phase 0 local scorecard, with optional `PROJECTS_DIR=path` / `--projects-dir`, `D3_GOLD=d3_gold.json` / `--d3-gold-file`, `D3_BASELINES=d3_baselines.json` / `--d3-baselines-file`, `GOLD=gold.json` / `--gold-file`, `BASELINES=baselines.json` / `--d7-baselines-file`, `PROMPT_INJECTION=inv7.json` / `--prompt-injection-file`, `D6_PROTOCOL=protocol.json` / `--d6-bias-protocol-file`, `D4_PROTOCOL=protocol.json` / `--d4-codebook-quality-protocol-file`, `D8_PROTOCOL=protocol.json` / `--d8-gt-fidelity-protocol-file`, `D9_PROTOCOL=protocol.json` / `--d9-interpretive-preference-protocol-file`, `CONFIDENCE_PROTOCOL=protocol.json` / `--confidence-calibration-protocol-file`, `BIAS_COUNTERFACTUAL=bias.json` / `--bias-counterfactual-file`, `BIAS_STRATIFIED=bias_stratified.json` / `--bias-stratified-file`, `CODEBOOK_QUALITY=quality.json` / `--codebook-quality-file`, `GT_FIDELITY=gt_fidelity.json` / `--gt-fidelity-file`, `PREFERENCE=preference.json` / `--interpretive-preference-file`, `CALIBRATION=calibration.json` / `--confidence-calibration-file`, `OBS_DB=` / `--observability-db` plus `TRACE_ID=` / `--trace-id` overrides for D10 cost/latency, and `ARTIFACT_DIR=benchmark_results` / `--artifact-dir` for a versioned Phase 0 package. `qc_cli.py bench` accepts the same Phase 0 file/protocol long options as `scripts/bench_phase0.py`; the Make variables are aliases over that canonical script surface. `D6_PROTOCOL` runs D6 protocol/result preflight at the score boundary and blocks scorecard/output/artifact writes when it fails; `D4_PROTOCOL` does the same for D4 codebook-quality protocol/result preflight; `D8_PROTOCOL` does the same for D8 GT-fidelity protocol/result preflight; `D9_PROTOCOL` does the same for D9 interpretive-preference protocol/result preflight; `CONFIDENCE_PROTOCOL` does the same for confidence-calibration protocol/result preflight. `make validate-d4-codebook-quality-protocol PROTOCOL=protocol.json` / `scripts/validate_d4_codebook_quality_protocol.py protocol.json` validates pre-evaluation D4 codebook-quality protocol metadata; `make d4-codebook-quality-preflight PROTOCOL=protocol.json QUALITY=quality.json` / `scripts/preflight_d4_codebook_quality_protocol.py protocol.json --quality-file quality.json` checks concrete D4 result files against that protocol before scoring; `make validate-d6-bias-protocol PROTOCOL=protocol.json` / `scripts/validate_d6_bias_protocol.py protocol.json` validates pre-run D6 bias-audit protocol metadata; `make d6-bias-preflight PROTOCOL=protocol.json STRATIFIED=bias_stratified.json COUNTERFACTUAL=bias_counterfactual.json` / `scripts/preflight_d6_bias_protocol.py protocol.json --stratified-file bias_stratified.json --counterfactual-file bias_counterfactual.json` checks concrete D6 result files against that protocol before scoring; `make validate-d8-gt-fidelity-protocol PROTOCOL=protocol.json` / `scripts/validate_d8_gt_fidelity_protocol.py protocol.json` validates pre-evaluation D8 GT-fidelity protocol metadata before rubric outcomes are collected or scored; `make d8-gt-fidelity-preflight PROTOCOL=protocol.json GT_FIDELITY=gt_fidelity.json` / `scripts/preflight_d8_gt_fidelity_protocol.py protocol.json --gt-fidelity-file gt_fidelity.json` checks concrete D8 result files against that protocol before scoring; `make validate-confidence-calibration-protocol PROTOCOL=protocol.json` / `scripts/validate_confidence_calibration_protocol.py protocol.json` validates pre-evaluation confidence-calibration protocol metadata before correctness labels are collected or scored; `make confidence-calibration-preflight PROTOCOL=protocol.json CALIBRATION=calibration.json` / `scripts/preflight_confidence_calibration_protocol.py protocol.json --calibration-file calibration.json` checks concrete calibration result files against that protocol before scoring; `make bench CONFIDENCE_PROTOCOL=protocol.json CALIBRATION=calibration.json` / `scripts/bench_phase0.py --confidence-calibration-protocol-file protocol.json --confidence-calibration-file calibration.json` enforces the same preflight at score time; `make validate-theoretical-sampling-protocol PROTOCOL=protocol.json` / `scripts/validate_theoretical_sampling_protocol.py protocol.json` validates INV-4 theoretical-sampling protocol metadata, and `make theoretical-sampling-preflight PROTOCOL=protocol.json CANDIDATES=candidates.json [RESULTS=results.json]` / `scripts/preflight_theoretical_sampling_protocol.py protocol.json --candidates-file candidates.json [--results-file results.json]` checks concrete candidate/result packages against protocol IDs, project/corpus/state hashes, source policy, collection mode, target-gap coverage, and selected-candidate provenance; `make validate-adjudication-protocol PROTOCOL=protocol.json` / `scripts/validate_adjudication_protocol.py protocol.json` validates pre-registered protocol metadata; `make adjudication-protocol-preflight PROTOCOL=protocol.json SAMPLE=sample.json` / `scripts/preflight_adjudication_protocol_sample.py protocol.json sample.json` checks protocol/sample package matching before labeling; `make adjudication-sample ID=<project_id> OUTPUT=sample.json` / `qc_cli.py project adjudication-sample <project_id> --output-file sample.json` exports unlabeled schema_version=1 review packets with project/corpus hashes, source context, and response templates for human/expert adjudicators; `make validate-adjudication-responses PACKAGE=sample.json` / `scripts/validate_adjudication_responses.py sample.json` validates completed response package shape/completeness; `make adjudication-response-preflight PROTOCOL=protocol.json SAMPLE=sample.json RESPONSES=responses.json` / `scripts/preflight_adjudication_responses.py protocol.json sample.json responses.json` checks that completed responses still match the registered protocol/sample provenance and exact item IDs before import; `make import-adjudication-responses PACKAGE=sample.json ... PREFLIGHT_PROTOCOL=protocol.json PREFLIGHT_SAMPLE=sample.json` / `scripts/import_adjudication_responses.py sample.json ... --protocol-package protocol.json --sample-package sample.json` converts valid completed responses into D3/D7 package inputs when explicitly requested, and can run the same preflight as an import-time guard before writing outputs. `make write-phase0-adjudication-package ID=<project_id> OUTPUT=phase0_package.json [D3_GOLD=d3_gold.json] [GOLD=d7_gold.json]` / `scripts/write_phase0_adjudication_package.py` validates those versioned package files together and writes the strict manifest. `qc_cli.py bench-package phase0_package.json` / `make bench-package PACKAGE=phase0_package.json` / `scripts/run_phase0_benchmark_package.py phase0_package.json` validates a `schema_version=1` manifest and resolves listed input paths relative to that manifest before invoking the same canonical Phase 0 bench path. `make run-d7-retrieval`, `make validate-d7-comparison-protocol PROTOCOL=protocol.json`, `make d7-comparison-preflight PROTOCOL=protocol.json GOLD=gold.json PREDICTIONS="a.json b.json"`, and `make compare-d7-retrieval ... [PROTOCOL=protocol.json]` can produce/validate/preflight/guard/score D7 retrieval comparison inputs. `make run-inv7-fixtures`, `make validate-inv7-live-protocol PROTOCOL=protocol.json`, `make run-inv7-live-fixtures`, `make inv7-live-preflight PROTOCOL=protocol.json PACKAGE=inv7_live.json`, and `make validate-inv7-package PACKAGE=inv7.json` can produce/validate/preflight structural or live INV-7 protocol/result inputs for `PROMPT_INJECTION=`. The scorecard includes `_meta.input_hashes` for the loaded project state, corpus, and supplied benchmark files plus `_meta.run_configuration_hashes` for persisted methodology/model/config metadata; prompt hashes are explicitly `not_run` because Phase 0 is LLM-free. Target future behavior behind `qc bench` is a frozen `prompt_eval` suite against a dataset that writes a scored report. Agent-drivable end to end (no manual steps). The package runner, D4 codebook-quality protocol validator/result preflight/score-time guard, D6 bias protocol validator/result preflight/score-time guard, D8 GT-fidelity protocol validator/result preflight/score-time guard, D9 interpretive-preference protocol/result preflight/score-time guard, confidence-calibration protocol/result preflight/score-time guard, theoretical-sampling protocol/candidate/result preflight, D7 comparison protocol/preflight/guard, adjudication protocol validation/preflight, sample export/response validation/response preflight/guarded import/package assembly, and INV-7 live protocol/result package validators/preflight are orchestration/provenance only, not populated held-out benchmark evidence.
- **INV-9 claim-anchor coverage:** Phase 0 scorecards include `claim_anchor_coverage`, a deterministic summary of total claims, supporting/contrary anchor counts, anchored versus unanchored claim counts, Wilson interval metadata for anchored rate, and breakdowns by claim kind, source stage, and support status. This is structural source-anchor accounting only; it does not prove claim truth, human adjudication, disconfirmation coverage, methodological validity, or SOTA evidence.
- **Corpus-scope adequacy:** Phase 0 scorecards include `corpus_scope_adequacy`, a deterministic summary of scope-record status (`missing`, `empty`, `missing_sampling_frame`, or `complete`), field completeness, document count, claim count, and status-specific warnings. This is report-boundary accounting only; it does not validate sampling-frame adequacy, population generalization, methodological validity, or SOTA evidence.
- **Theoretical sampling package surfaces:** `make export-theoretical-sampling-candidates ID=<project_id> PROTOCOL=protocol.json OUTPUT=candidates.json` / `scripts/export_theoretical_sampling_candidates.py <project_id> --protocol protocol.json --output candidates.json` exports loaded-document candidate packages from diagnostic suggestions for `make theoretical-sampling-preflight`. `make export-theoretical-sampling-results PROTOCOL=protocol.json CANDIDATES=candidates.json SELECTED=candidate_id SUCCESS_CRITERION="..." OUTPUT=results.json` / `scripts/export_theoretical_sampling_results.py protocol.json --candidates-file candidates.json --selected-candidate-id candidate_id --success-criterion-met "..." --output results.json` records explicit selected candidate IDs as result packages. These are package/provenance exports only, not sampling execution, data collection, sampling-frame adequacy evidence, methodological saturation evidence, or GT-fidelity evidence.
- **D7 retrieval CLI aliases:** `qc_cli.py run-d7-retrieval <project_id> ...` delegates to `scripts/run_d7_retrieval.py`, `make validate-d7-baseline-package PACKAGE=...` and `qc_cli.py validate-d7-baseline-package ...` validate retrieval/live-baseline package files, and `qc_cli.py compare-d7-retrieval <project_id> --gold-file gold.json --predictions-file predictions.json [--protocol-package protocol.json]` delegates to `scripts/compare_d7_retrieval.py`. These are canonical surfaces for the existing D7 retrieval export/package-validation/comparison substrate, not held-out D7 evidence or live-baseline evidence by themselves.
- **D7 live baseline CLI aliases:** `qc_cli.py run-d7-live-baseline <project_id> --output live_baseline.json --model ...` delegates to `scripts/run_d7_live_baseline.py`, which writes an opt-in live candidate-selection baseline package for `BASELINES=`. D7 comparison protocol/preflight can now guard those live packages when the protocol declares `baseline_mode="live_candidate_selector"` and the expected model/config metadata. This is a package generator and preflight substrate over bounded retrieval candidates, not a committed held-out D7 result, unbounded generic ChatGPT baseline, or superiority claim.
- **INV-7 fixture CLI aliases:** `qc_cli.py run-inv7-fixtures --output inv7.json` delegates to `scripts/run_inv7_fixtures.py`, and `qc_cli.py run-inv7-live-fixtures --output inv7_live.json --model ...` delegates to `scripts/run_inv7_live_fixtures.py`. `qc_cli.py validate-inv7-package`, `qc_cli.py validate-inv7-live-protocol`, and `qc_cli.py inv7-live-preflight` delegate to the canonical validation/preflight scripts. These generate or validate scorecard-compatible fixture packages only; the committed built-in canary artifact is inspectable but is still not prompt-injection robustness proof or held-out benchmark evidence.
- **D9 protocol input surface:** `make validate-d9-interpretive-preference-protocol PROTOCOL=protocol.json` / `scripts/validate_d9_interpretive_preference_protocol.py protocol.json` validates pre-evaluation D9 blind preference protocol metadata before forced-choice outcomes are collected or scored. `make d9-interpretive-preference-preflight PROTOCOL=protocol.json PREFERENCE=preference.json` / `scripts/preflight_d9_interpretive_preference_protocol.py protocol.json --preference-file preference.json` checks concrete D9 preference results against the registered protocol before scoring. `make bench D9_PROTOCOL=protocol.json PREFERENCE=preference.json` / `scripts/bench_phase0.py --d9-interpretive-preference-protocol-file protocol.json --interpretive-preference-file preference.json` enforces the same preflight at score time, blocks scorecard/output/artifact writes when it fails, records `_meta.preflight_reports.d9_interpretive_preference` when it passes, and uses the supplied protocol metadata for local D9 non-inferiority assessment without mutating saved project state. These validators and guards are orchestration/provenance only, not populated held-out benchmark evidence.
- **D3 baseline input surface:** D3 baseline predictions use `D3_BASELINES=d3_baselines.json` / `--d3-baselines-file` / package manifest `d3_baselines_file`, with payloads shaped as a JSON list of baseline records, a legacy object containing `application_baselines`, or a schema_version=1 package with `package_type="qualitative_coding.d3_application_baseline_predictions"`. `make validate-d3-baseline-package PACKAGE=...` validates versioned packages; package validation is provenance/accounting only, not held-out D3 evidence.
- **Outputs:** Phase 0 scorecards now include input hashes for the loaded project state, corpus, and supplied external files plus run-configuration hashes for persisted methodology/model/config metadata. When `--artifact-dir` / `ARTIFACT_DIR=` is supplied, Phase 0 writes a versioned run directory with `scorecard.json` and `manifest.json`; the manifest records input hashes, run-configuration hashes, scorecard hash, CLI provenance, claim-discipline text, and explicit `prompt_hashes.status=not_run` / `prompt_eval.status=not_run` caveats. A Phase 0 package manifest can drive those same outputs from one file, with relative paths resolved from the manifest directory. The target full harness output remains a richer `benchmark_results/` package with per-dimension scores, CIs, baseline deltas, dataset + prompt + model hashes, and a generated scorecard. These hashes are the artifact evidence the theory doc's claim discipline requires before any public SOTA statement; Phase 0 hashes and packages are provenance metadata, not validity evidence.

## 7. Acceptance criteria (the SOTA gate)

Two different statistical tests, because we make two different kinds of claim — do not conflate them:

- **Superiority** (for "more X than existing methods", e.g. D1/D2/D7): licensed only when the system is ≥ the named baseline with a **CI that excludes the baseline** (a one-sided superiority test).
- **Non-inferiority / parity** (for "matches expert humans", e.g. D9 and D3 where parity is the claim): licensed only when the system is within a **pre-registered non-inferiority margin** of the human ceiling — i.e. the CI for (system − human) lies above −δ for a δ fixed *before* the run. A superiority test is the wrong tool for a parity claim; using "CI excludes baseline" everywhere would either over-reject parity or smuggle in an unearned superiority claim.

Both require a held-out, prompt-frozen, contamination-checked dataset, recorded with dataset/prompt/model hashes. Until a dimension passes its appropriate test, claim discipline (theory doc §14) applies: describe capability, not superiority/parity.

## 8. Phasing (smallest real slice first)

- **Phase 0 local scorecard — DONE.** `qc_cli.py bench <project>` / `make bench ID=<project>` (`qc_clean/core/bench.py` → `phase0_scorecard`) computes **D1 grounding** with a local Wilson interval (now real: `verify_grounding` returns the fraction of applications whose span anchor verifies — INV-1 landed), **D2 coverage** with local Wilson intervals (including examined-and-judged coverage under `--exhaustive` and coded-segment rate over examined decisions), surfaces externally supplied **D4 codebook-quality** rubric outcomes with local rubric-mean bootstrap intervals through `CODEBOOK_QUALITY=quality.json`, can validate pre-evaluation D4 protocol metadata (`make validate-d4-codebook-quality-protocol PROTOCOL=protocol.json`), can preflight D4 result files against that protocol (`make d4-codebook-quality-preflight PROTOCOL=protocol.json QUALITY=quality.json`), and can enforce that D4 preflight at score time when `D4_PROTOCOL=...` / `--d4-codebook-quality-protocol-file` is supplied, surfaces **D5** reliability/stability with Gwet's AC1, prevalence tables, and local row-bootstrap intervals when present, includes a gold-dependent exact code/source-anchor **D3 application-validity** score with recall/precision Wilson intervals, F1 bootstrap intervals, local same-code/doc span-IoU plus Modified Hausdorff diagnostics, exact-key binary system-vs-gold percent agreement/κ/AC1/Krippendorff's α plus prevalence, and human-ceiling exact-metric comparison plus human-human κ/α/AC1 metadata when supplied through a versioned `D3_GOLD=d3_gold.json` package, includes externally supplied **D6 counterfactual bias** scoring with code-change-rate Wilson intervals through `BIAS_COUNTERFACTUAL=bias.json` and externally supplied **D6 stratified bias** scoring with accuracy/error-rate Wilson intervals, per-attribute/per-group summaries, per-surface summaries, and max error-rate gaps through `BIAS_STRATIFIED=bias_stratified.json`, can validate pre-run D6 bias-audit protocol metadata (`make validate-d6-bias-protocol PROTOCOL=protocol.json`), can preflight D6 result files against that protocol (`make d6-bias-preflight PROTOCOL=protocol.json STRATIFIED=... COUNTERFACTUAL=...`), and can enforce that preflight at score time when `D6_PROTOCOL=...` / `--d6-bias-protocol-file` is supplied, includes a gold-dependent **D7 disconfirmation** exact-anchor score with recall/precision Wilson intervals, F1 bootstrap intervals, exact-key binary system-vs-gold percent agreement/κ/AC1/Krippendorff's α plus prevalence, local paired baseline-delta bootstrap intervals, and human-ceiling exact-metric comparison plus human-human κ/α/AC1 metadata when supplied through a versioned `GOLD=gold.json` package, can score externally supplied D7 baseline predictions through `BASELINES=baselines.json`, reports externally supplied **INV-7 prompt-injection** fixture outcomes with pass/attack-rate Wilson intervals plus per-surface/per-attack-type summaries when supplied through `PROMPT_INJECTION=inv7.json` (raw list, object with `prompt_injection_evaluations`, or schema_version=1 INV-7 package), includes an externally supplied **D8 GT-fidelity** scorecard with local rubric-mean bootstrap intervals through `GT_FIDELITY=gt_fidelity.json`, can validate pre-evaluation D8 protocol metadata (`make validate-d8-gt-fidelity-protocol PROTOCOL=protocol.json`), can preflight D8 result files against that protocol (`make d8-gt-fidelity-preflight PROTOCOL=protocol.json GT_FIDELITY=gt_fidelity.json`), and can enforce that preflight at score time when `D8_PROTOCOL=...` / `--d8-gt-fidelity-protocol-file` is supplied, includes an externally supplied **D9 interpretive-preference** scorecard through `PREFERENCE=preference.json`, can validate/preflight D9 protocol metadata (`make validate-d9-interpretive-preference-protocol PROTOCOL=protocol.json`, `make d9-interpretive-preference-preflight PROTOCOL=protocol.json PREFERENCE=preference.json`), and can enforce that D9 preflight at score time when `D9_PROTOCOL=...` / `--d9-interpretive-preference-protocol-file` is supplied, includes an externally supplied **confidence-calibration** scorecard with Brier/ECE bootstrap intervals through `CALIBRATION=calibration.json`, can validate/preflight/enforce confidence-calibration protocol metadata (`make validate-confidence-calibration-protocol PROTOCOL=protocol.json`, `make confidence-calibration-preflight PROTOCOL=protocol.json CALIBRATION=calibration.json`, `make bench CONFIDENCE_PROTOCOL=protocol.json CALIBRATION=calibration.json`), reports **D10 cost/latency** from matching `llm_client` observability rows, reports **D10 wall-clock runtime** from the last local `project run`, includes `_meta.input_hashes` for project/corpus/external inputs and `_meta.run_configuration_hashes` for persisted config/model metadata, can run from a versioned Phase 0 package manifest (`make bench-package PACKAGE=...`), can validate pre-label adjudication protocol metadata (`make validate-adjudication-protocol PROTOCOL=protocol.json`), can preflight a protocol against a concrete sample package (`make adjudication-protocol-preflight PROTOCOL=protocol.json SAMPLE=sample.json`), can export unlabeled adjudication sample packets (`make adjudication-sample ID=<project> OUTPUT=sample.json`), can validate completed adjudication response package shape/completeness (`make validate-adjudication-responses PACKAGE=sample.json`), can preflight completed responses against protocol/sample provenance (`make adjudication-response-preflight PROTOCOL=protocol.json SAMPLE=sample.json RESPONSES=responses.json`), can import valid completed responses into D3/D7 package inputs with optional import-time preflight guard inputs (`make import-adjudication-responses ... PREFLIGHT_PROTOCOL=protocol.json PREFLIGHT_SAMPLE=sample.json`), can write a strict manifest from imported D3/D7 package inputs (`make write-phase0-adjudication-package ...`), and can write a versioned Phase 0 `benchmark_results/` package containing `scorecard.json`, `timing_d10.json` with runtime environment metadata, and manifest hashes. Deterministic and LLM-free unless a separate opt-in live INV-7 fixture file or embedding retrieval export is generated before scoring; prompt hashes are explicitly `not_run` until the future `prompt_eval` suite executes frozen prompts. Remaining harness work: wire into `prompt_eval` for frozen case sets beyond the local exact-key bootstrap, build/run held-out D3/D7 gold sets from real adjudication protocols, run D4 LLM-judge and blind expert-panel evaluations, add full D3 semantic/multi-label Krippendorff's α agreement and populated human-ceiling comparison data, run a populated prompt_eval-backed D6 bias suite, collect D8 expert GT-fidelity rubric outcomes under a pre-registered protocol, collect blind expert D9 preference outcomes under a pre-registered non-inferiority margin, populate held-out confidence-calibration labels, run actual/live baselines on held-out data, run broader held-out live adversarial prompt-injection fixtures beyond the built-in canary against a committed benchmark protocol, turn public benchmark timing into full benchmark artifacts with controlled environment/baseline context, and run on reused public gold as a comparator.
  D9 protocol metadata can now be validated before outcome collection, D9
  result files can now be preflighted against those protocols, and `make bench
  D9_PROTOCOL=... PREFERENCE=...` can enforce that preflight while using the
  protocol metadata for a local non-inferiority margin assessment, but no D9
  parity claim is licensed without populated held-out blind expert outcomes.
  It can also score externally supplied D3 baseline predictions through `D3_BASELINES=d3_baselines.json`, reporting exact-key system-minus-baseline deltas and local paired bootstrap intervals when D3 gold is present.
- **Phase 1 — small adjudicated gold.** Build/borrow a small expert gold set; compute D3 (κ/α, IoU/Hausdorff), D4 (codebook quality), D7 (disconfirmation recall) vs the human ceiling.
- **Phase 2 — full benchmark + public scorecard.** Add baselines (ChatGPT, commercial, prototypes), populated D6 stratified/counterfactual bias protocols, confidence calibration, D8 (GT fidelity), D9 (interpretive-depth preference). Publish the scorecard with the SOTA gate (§7) applied per dimension.

## 9. Threats to the evaluation itself

- **Benchmark construct validity** — does each metric measure the dimension it names? (cf. "Measuring what Matters," arXiv:2511.04703). Document the construct for each.
- **Gold disagreement** — humans disagree; always report the human–human ceiling, never treat one coder as truth.
- **Train/test contamination** — public datasets may be in training data; flag, and base headline claims on fresh in-house gold.
- **Prompt overfitting** — freeze prompts/models and hold out test sets; `instruction_search` optimizes on a *separate* dev split only.
- **LLM-judge bias** — `llm_judge` (D4) can favor LLM-style prose; cross-check against the blind human panel.

## 10. References

Human IRR bands and gold-via-consensus method: qualitative content-analysis IRR literature; PLOS Digital Health / medRxiv blinded comparison (expert consensus panels). Span-alignment metrics + released human-coded data: LLMCode (Oksanen, Lucero, Hämäläinen). Hierarchical-coding head-to-head: HICode (Zhong, Wang, Field, EMNLP 2025). GT-style schema alignment: LOGOS (Pi, Yang, Nguyen). Systematic-bias caution: Ashwin, Chhabra & Rao (2025, *Sociological Methods & Research*). Benchmark construct-validity caution: "Measuring what Matters" (arXiv:2511.04703). Stats + experiment infra: `prompt_eval` (bootstrap CI, Welch's test, evaluators). See `PROJECT_THEORY_AND_GOALS.md` §11/§15 for the consistency-vs-validity and unit-of-analysis context this harness resolves.

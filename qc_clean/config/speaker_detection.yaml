# Speaker Detection Configuration
speaker_detection:
  method: "regex"  # Options: "regex", "llm"
  fallback: "regex"
  max_tokens: 2000
  circuit_breaker:
    failure_threshold: 5
    timeout_seconds: 30
  performance:
    max_response_time: 10.0  # seconds
    token_budget: 1000
# qc_clean/plugins

Plugin entrypoints for `qualitative_coding`.

Use this subtree for externally exposed surfaces such as the API and browser
UIs. Keep transport/plugin concerns here rather than in the pipeline core.

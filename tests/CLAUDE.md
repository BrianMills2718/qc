# tests

Automated coverage for `qualitative_coding`.

Use this subtree for unit and end-to-end verification of the pipeline, API, and
export surfaces. Prefer real `ProjectState` flows over brittle mock-heavy
tests.
